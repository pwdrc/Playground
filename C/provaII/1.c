// Pedro Guilherme Tolvo
// 10492012

/**

  1) Na passagem por valor é passado, exatamente, o valor armazenado na variável.
  Iniciais:
  	a = x;
	b = y;
  Finais:
  	a = x;
	b = y;

  2) Na passagem por referências, é passado, exatamente, o endereço da memória da variável, o que permite a "comunicação" entre a função principal e a swap.
  Iniciais:
  	a = x;
	b = y;
  Finais:
  	a = y;
	b = x;

 **/
